INCLUDE_DIRECTORIES(../include/)

SET(C4_SRC ${C4_SRC}
  c4.cxx
)

ADD_LIBRARY(C4 SHARED ${C4_SRC})

INSTALL(TARGETS C4
        RUNTIME DESTINATION bin
        ARCHIVE DESTINATION lib
        LIBRARY DESTINATION lib
        )

