INCLUDE_DIRECTORIES(../include/)

SET(C4_SRC ${C4_SRC}
  KeyKeyValueStore.cpp
)

ADD_LIBRARY(C4 ${C4_SRC})
TARGET_LINK_LIBRARIES(C4 ZETER)

INSTALL(TARGETS C4
        RUNTIME DESTINATION bin
        ARCHIVE DESTINATION lib
        LIBRARY DESTINATION lib
        )

